# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
**/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage
**/coverage

# next.js
/.next/
**/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts
.env*.local

# Playwright
node_modules/
/test-results/
**/test-results/
/playwright-report/
**/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Agent instruction files. `next dev` rewrites these whenever it detects an AI
# coding agent, and the block it writes has nothing project-specific in it.
# The regenerated copies stay usable locally; they just never enter git.
# Use `git add -f` if a hand-written one is ever worth tracking.
AGENTS.md
CLAUDE.md
