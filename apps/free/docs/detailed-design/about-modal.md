# AboutModal 상세설계

소스: `components/AboutModal.tsx`

`AboutModal`은 우측 상단 `BoardMenu`의 About 항목에서 연다. 개발자 이메일, GitHub 프로필, 블로그 링크를 제공한다. 이메일은 기본 메일 앱으로 연결하고 GitHub와 블로그는 새 탭으로 연다.

닫기 버튼, 모달 바깥 영역 클릭 또는 `Escape` 키로 닫을 수 있다.
